# GitHub Pages 部署步骤

## 📋 准备工作（已完成）

✅ Git 用户信息已配置
✅ 静态文件已生成
✅ 部署插件已安装
✅ 配置文件已更新

---

## 🚀 部署步骤

### 方法一：使用 Hexo 自动部署（推荐）

在 PowerShell 中运行以下命令：

```powershell
hexo deploy
```

当提示输入时：
1. **Username**：输入 `amrgafs778`
2. **Password**：输入你的 Personal Access Token：
   ```
   你的Personal Access Token（请在GitHub设置中生成）
   ```

⚠️ **注意**：输入 Token 时不会显示任何字符，这是正常的，直接粘贴即可。

---

### 方法二：手动部署（如果方法一失败）

如果 `hexo deploy` 失败，可以手动部署：

#### 1. 进入 public 目录
```powershell
cd public
```

#### 2. 初始化 Git 仓库
```powershell
git init
```

#### 3. 添加所有文件
```powershell
git add .
```

#### 4. 提交更改
```powershell
git commit -m "Initial commit"
```

#### 5. 添加远程仓库
```powershell
git remote add origin https://github.com/amrgafs778/amrgafs778.github.io.git
```

#### 6. 推送到 GitHub
```powershell
git push -u origin main
```

当提示输入时：
- **Username**：`amrgafs778`
- **Password**：你的 Token

---

## 🎯 部署后验证

### 1. 等待部署完成
- 部署完成后，访问：`https://amrgafs778.github.io`
- 首次访问可能需要等待 1-5 分钟

### 2. 检查 GitHub Pages 设置
1. 访问：https://github.com/amrgafs778/amrgafs778.github.io/settings/pages
2. 确认 **Source** 设置为：
   - Branch: `main`
   - Folder: `/ (root)`

### 3. 测试访问
在浏览器中打开：`https://amrgafs778.github.io`

---

## 🔄 后续更新博客

### 创建新文章
```powershell
hexo new "文章标题"
```

### 编辑文章
编辑 `source/_posts/` 目录下的 Markdown 文件

### 预览效果
```powershell
hexo server
```
访问 http://localhost:4000 预览

### 部署更新
```powershell
hexo clean
hexo generate
hexo deploy
```

---

## ⚠️ 常见问题

### Q1: 部署失败，提示认证错误
**解决方案**：
- 确认 Token 没有过期
- 确认 Token 有 `repo` 和 `workflow` 权限
- 重新生成 Token 并尝试

### Q2: 访问博客显示 404
**解决方案**：
- 等待 5-10 分钟，GitHub Pages 需要时间部署
- 检查仓库名是否正确（必须是 `amrgafs778.github.io`）
- 检查仓库是否为公开

### Q3: 如何查看部署状态？
**解决方案**：
- 访问仓库的 Actions 页面：https://github.com/amrgafs778/amrgafs778.github.io/actions
- 查看部署工作流的状态

---

## 📝 重要信息

- **GitHub 用户名**：amrgafs778
- **仓库名**：amrgafs778.github.io
- **博客地址**：https://amrgafs778.github.io
- **Token**：已保存（请妥善保管）

---

## 🎉 完成！

部署成功后，你就可以：
- ✅ 在任何地方访问你的博客
- ✅ 分享博客地址给朋友
- ✅ 在手机、平板、电脑上访问
- ✅ 24小时在线，无需电脑开机

祝你博客部署成功！🎊
